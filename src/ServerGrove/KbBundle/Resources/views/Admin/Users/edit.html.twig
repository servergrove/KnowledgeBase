{% extends "ServerGroveKbBundle:Admin:layout.html.twig" %}

{% block postheader %}
    {{ parent() }}
    {{ sgkb_breadcrumb.displayBreadcrumb(false, false, true) }}
{% endblock postheader %}

{% block content %}
<form class="form-horizontal" action="{{ path('sgkb_admin_users_update', { 'username': document.username }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form, { legend: 'User edit'|trans } ) }}

    <div class="form-actions">
        <button class="btn btn-primary" type="submit">{% trans %}Save{% endtrans %}</button>
        <a class="btn" href="{{ path('sgkb_admin_users_index') }}">{% trans %}Cancel{% endtrans %}</a>
    </div>
</form>
{% endblock %}
